```text
Status: TRANSPILED
Source: README / Machine³
Target: Infant
Language: 639-1:en / 3166-2:GB
```

There is a heading at the top.

There are two paragraphs. The paragraphs have some words that look different, like **Machine³** and **Logic³**.

There is a heading. Under it there is a smaller heading. Under that there is a table. The table has three columns. The table has nine rows. Each row has a language name and some words.

There are two links. Each link has a description next to it.

There is a heading. Under it there is a smaller heading. Under that there are four items in a list. Each item has a word in bold and more words after it.

There is a smaller heading. Under it there is a table with two columns and four rows.

There is a heading. Under it there are some paragraphs with links.

There is a line.

There is a box with words.

There is a line.

There is a heading with words. There is a heading with pictures.
